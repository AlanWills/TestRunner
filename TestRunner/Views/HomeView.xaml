<Page x:Class="TestRunner.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestRunner.UserControls"
      mc:Ignorable="d" 
      d:DesignWidth="1920"
      d:DesignHeight="1040">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <StackPanel Height="1040" VerticalAlignment="Top" DockPanel.Dock="Right" Width="400">
                <Button x:Name="button" Content="Button"/>
            </StackPanel>
            <GridSplitter x:Name="gridSplitter" VerticalAlignment="Stretch" ResizeDirection="Columns" DockPanel.Dock="Right" Width="5"/>
            <StackPanel Height="1040" VerticalAlignment="Top" DockPanel.Dock="Left">
                <Button x:Name="button1" Content="Button" Width="75"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
