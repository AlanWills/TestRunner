<Page
    x:Class="TestRunner.Views.NewConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestRunner.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="800"
    d:DesignHeight="600">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Frame Grid.Column="0">
            <local:NavBar />
        </Frame>
        <Frame Grid.Column="1" >
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Test Project Dll" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="FullPathToDll" FilePath="{Binding FullPathToDll, Mode=TwoWay}" FileExtension=".dll"></local:FileOpen>
                <TextBlock Text="Output Log File Path" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="OutputFileFullPath" FilePath="{Binding OutputFileFullPath, Mode=TwoWay}" FileExtension=".txt"></local:FileOpen>
                <TextBlock Text="Error Log File Path" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="ErrorFileFullPath" FilePath="{Binding ErrorFileFullPath, Mode=TwoWay}" FileExtension=".txt"></local:FileOpen>
                <StackPanel Orientation="Horizontal" HorizontalAlignment ="Center" Width="215">
                    <Button 
                        x:Name="StartButton" 
                        Content="Start" 
                        HorizontalAlignment="Left" 
                        Width="100"
                        Margin="0,0,7.5,0"
                        Click="CreateTestRunConfiguration"
                        ToolTipService.ToolTip="Launch a Test Run process on the service with these configurations."/>
                    <Button 
                        x:Name="LoadButton" 
                        Content="Load" 
                        HorizontalAlignment="Right" 
                        Width="100"
                        Margin="7.5,0,0,0"
                        Click="LoadTestRunConfiguration"
                        ToolTipService.ToolTip="Load a configuration file."/>
                </StackPanel>
            </StackPanel>
        </Frame>
    </Grid>
</Page>
