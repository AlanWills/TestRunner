<Page
    x:Class="TestRunner.Views.NewConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestRunner.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="800"
    d:DesignHeight="600">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Frame Grid.Column="0">
            <local:NavBar />
        </Frame>
        <Frame Grid.Column="1" >
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Test Project Dll" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="DllPathOpen" FilePath="{Binding FullPathToDll, Mode=TwoWay}" FileExtension=".dll"></local:FileOpen>
                <TextBlock Text="Output Log File Path" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="OutputFileOpen" FilePath="{Binding OutputFileFullPath, Mode=TwoWay}" FileExtension=".txt"></local:FileOpen>
                <TextBlock Text="Error Log File Path" Margin="20,10,0,0"></TextBlock>
                <local:FileOpen x:Name="ErrorFileOpen" FilePath="{Binding ErrorFileFullPath, Mode=TwoWay}" FileExtension=".txt"></local:FileOpen>
                <Button 
                    x:Name="StartButton" 
                    Content="Start" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Stretch" 
                    Width="100" 
                    Margin="0, 20, 20, 0"
                    Click="CreateTestRunProcess"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Launch a Test Run process on the service with these configurations."/>
            </StackPanel>
        </Frame>
    </Grid>
</Page>
