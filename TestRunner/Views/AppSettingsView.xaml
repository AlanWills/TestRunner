<Page
    x:Class="TestRunner.Views.AppSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TestRunner.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="1920"
    d:DesignHeight="1080">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Height="100" VerticalAlignment="Top" HorizontalAlignment="Center"  Margin="0,30,0,0" Grid.Row="0">
            <TextBlock x:Name="VSTestPathHelp" TextWrapping="Wrap" Text="Full path to VSTest.console.exe" HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" Width="240"/>
            <local:FileOpen x:Name="VSTestFileOpen" FilePath="{Binding VSTestPath, Mode=TwoWay}" FileExtension=".exe"/>
        </StackPanel>
    </Grid>
</Page>
